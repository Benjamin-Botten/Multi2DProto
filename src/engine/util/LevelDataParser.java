package engine.util;

import engine.world.Level;
import org.json.simple.*;

public class LevelDataParser {

	/**
	 * Parses out all the contents of a level data-file
	 * @param level
	 */
	public LevelDataParser(Level level) {
	}
}
